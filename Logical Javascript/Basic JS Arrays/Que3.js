
var strings=["1.2","2.3","3.4"];

    function numberArray(strings){
        var nums=strings.map(Number);
        document.write("Array of string into numbers :["+nums+"]");
    }

    numberArray(strings);
